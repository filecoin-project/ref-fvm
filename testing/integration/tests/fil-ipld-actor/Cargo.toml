[package]
name = "fil_ipld_actor"
version = "0.1.0"
edition = "2021"

[dependencies]
fvm_ipld_encoding = { version = "0.2.2", path = "../../../../ipld/encoding" }
fvm_sdk = { version = "2.0.0-alpha.1", path = "../../../../sdk" }
fvm_shared = { version = "2.0.0-alpha.2", path = "../../../../shared" }
minicov = {version = "0.2", optional = true}

[features]
coverage = ["minicov"]

[build-dependencies]
substrate-wasm-builder = "4.0.0"
