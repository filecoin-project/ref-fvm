[package]
name = "fil_ipld_actor"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
fvm_ipld_encoding = { version = "0.2.2", path = "../../../../ipld/encoding" }
fvm_sdk = { version = "2.0.0-alpha.5", path = "../../../../sdk" }
fvm_shared = { version = "2.0.0-alpha.4", path = "../../../../shared" }

[target.'cfg(coverage)'.dependencies]
minicov = "0.2"

[build-dependencies]
substrate-wasm-builder = "4.0.0"
