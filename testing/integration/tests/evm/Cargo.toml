[package]
name = "evm_contracts"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
ethers = { version = "1.0" }

[build-dependencies]
ethers = { version = "1.0", features = ["abigen"] }
ethers-core = "1.0"
ethers-solc = "1.0"
serde = "1.0"
serde_json = "1"
