[workspace]
members = [
    "fvm",
    "sdk",
    "shared",
    "lib/*",
    "actor",
    "examples/*",
    "examples/blockstore-cgo/user",

    "ipld",
    "ipld/blockstore",
    "ipld/hamt",
    "ipld/amt",
]
exclude = [
    "examples/blockstore-cgo"
]

default-members = [
    "fvm",
    "sdk",
    "shared",
    "lib/*",
    "actor",
    # don't build the examples by default, as they require a specific target and profile.
    # build them with cargo make examples
    # "examples/*"
]

[patch.crates-io]
multihash = { git = "https://github.com/multiformats/rust-multihash", branch = "next-const-fns" }
cid = { git = "https://github.com/multiformats/rust-cid", branch = "next-const" }
forest_ipld = { path = "./ipld" }
ipld_blockstore = { path = "./ipld/blockstore" }
ipld_hamt = { path = "./ipld/hamt" }
ipld_amt = { path = "./ipld/amt" }
fvm_shared = { path = "./shared" }
libipld = { git = "https://github.com/ipfs-rust/libipld", branch = "steb/next" }

[profile.actor]
inherits = "release"
panic = "abort"
overflow-checks = true
lto = true
opt-level = "z"
#strip = true
